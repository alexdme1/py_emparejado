# ── Datos de imágenes (pesados, no van en el repo) ──
frontales/
traseras/
dataset_final/
paired_images/

# ── Estado de sesión (generado por la app) ──
state.json

# ── Frontend deps (pesado, se regenera con npm install) ──
frontend/node_modules/

# ── NOTA: frontend/dist/ SÍ se sube al repo (pre-built)
# ── para que no se necesite Node.js en producción ──

# ── Credenciales (NUNCA subir) ──
*.credentials.json
credentials.json
token.pickle

# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.egg-info/
dist/
!frontend/dist/
build/
*.egg

# ── Entornos virtuales ──
venv/
.venv/
env/
.env/

# ── IDE / Editores ──
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Gemini / AI agents ──
.gemini/
.agents/

# ── YOLO model cache ──
yolov8n.pt
